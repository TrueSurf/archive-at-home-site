---
import BaseLayout from '../layouts/BaseLayout.astro';
import { links } from '../data/site';
---

<BaseLayout title="贡献节点" bg="node" bgPosition="center 30%">
  <div class="container prose page-shell">
    <div class="section-title">
      <span class="section-mark"></span>
      <h2>贡献节点</h2>
    </div>
    <p class="section-desc">
      节点通过 WebSocket 连接中控服务器，领取任务后用自己的 E-Hentai 账号配额完成解析并回报结果。
      网络的解析能力全部来自志愿者节点。
    </p>

    <h2 id="prepare">准备工作</h2>
    <ul>
      <li><strong>一台能长期在线的设备</strong>：NAS、软路由、VPS、常开的电脑都可以，节点程序很轻量。</li>
      <li>
        <strong>E-Hentai 账号的 Cookie</strong>：登录 e-hentai.org 后从浏览器复制，至少包含
        <code>ipb_member_id</code> 和 <code>ipb_pass_hash</code>；非捐赠账号若无法自动进入 ExHentai，还需补上 <code>igneous</code>。
        可借助<a href="/tools">工具页</a>的 Cookie 工具提取。
      </li>
      <li>
        <strong>节点凭证</strong>：节点用 ED25519 签名认证。到<a href={links.telegramChannel} target="_blank" rel="noopener">Telegram 频道</a>联系管理员，
        获取分配给你的 <code>Node ID</code> 和 <code>Signature</code>。
      </li>
    </ul>

    <h2 id="config">编写 config.yaml</h2>
    <p>参考配置如下，逐项说明见下表：</p>
    <div class="codeblock">
      <span class="codeblock-tag">config.yaml</span>
      <pre>{`# 中控服务器
server:
  url: "wss://api.archive-at-home.org/ws"

# 节点凭证（联系管理员获取）
node:
  id: "node-001"
  signature: "base64-signature"

# E-Hentai 账号
ehentai:
  cookie: "ipb_member_id=xxx; ipb_pass_hash=xxx; igneous=xxx"
  max_gp_cost: -1            # 每日 GP 消耗上限，-1 为不限制

# 任务策略
task:
  gp_cost_willingness: 3     # GP 消耗意愿（1-5），越高越优先接到 GP 任务

# 本地数据（SQLite 解析日志）
database:
  path: "./data/db.db"

# 节点 Dashboard
dashboard:
  enabled: true
  address: ":8090"`}</pre>
    </div>

    <table>
      <thead><tr><th>配置项</th><th>说明</th></tr></thead>
      <tbody>
        <tr><td><code>server.url</code></td><td>中控服务器的 WebSocket 地址，官方网络为 <code>wss://api.archive-at-home.org/ws</code></td></tr>
        <tr><td><code>node.id / node.signature</code></td><td>管理员分配的节点标识与 ED25519 签名（Base64），一对一，请勿混用</td></tr>
        <tr><td><code>ehentai.cookie</code></td><td>完整 Cookie 字符串；系统会自动选择可用站点，无需手动指定表站/里站</td></tr>
        <tr><td><code>ehentai.max_gp_cost</code></td><td>每日 GP 消耗上限，到达后暂停接 GP 任务，<code>-1</code> 不限</td></tr>
        <tr><td><code>task.gp_cost_willingness</code></td><td>消耗意愿 1–5：调高更积极地消耗 GP</td></tr>
        <tr><td><code>database.path</code></td><td>SQLite 数据库路径，本地保存解析日志</td></tr>
        <tr><td><code>dashboard.address</code></td><td>Dashboard 监听地址，默认 <code>:8090</code></td></tr>
      </tbody>
    </table>

    <h2 id="run">启动节点</h2>
    <h3>方式一：Docker Compose（推荐）</h3>
    <p>从<a href={links.repoMain} target="_blank" rel="noopener">主仓库</a>的 <code>node/</code> 目录获取 <code>docker-compose.yml</code>，把写好的 <code>config.yaml</code> 放在同目录：</p>
    <div class="codeblock">
      <span class="codeblock-tag">bash</span>
      <pre>{`cd node
docker-compose up -d`}</pre>
    </div>

    <h3>方式二：二进制</h3>
    <div class="codeblock">
      <span class="codeblock-tag">bash</span>
      <pre>{`# 从 Releases 下载对应平台的包：https://github.com/Archive-At-Home/archive-at-home/releases
tar -xzf archive-at-home-node-*.tar.gz   # Windows 解压 .zip
cp config.yaml.example config.yaml       # 然后按上一节修改

./archive-at-home-node                   # Windows 运行 .exe`}</pre>
    </div>

    <h2 id="dashboard">Dashboard</h2>
    <p>
      浏览器打开 <code>http://localhost:8090</code> 查看节点 Dashboard：连接状态、任务与 GP 统计、
      历史累计（从 SQLite 读取），支持手动重连与刷新。显示已连接并开始接收任务即部署成功。
    </p>
    <p>
      节点与服务器之间的协议：服务器下发 <code>TASK_ASSIGNMENT</code>（画廊 ID + 密钥），
      节点执行后回报 <code>TASK_RESULT</code>（成功与否、实际 GP、归档链接），并周期性汇报
      <code>NODE_STATUS</code>（空闲配额、GP 余额、消耗意愿）供调度参考。
    </p>

    <h2 id="trouble">故障排查</h2>
    <table>
      <thead><tr><th>现象</th><th>排查方向</th></tr></thead>
      <tbody>
        <tr><td>连接失败</td><td>服务器地址是否为 <code>wss://</code> 开头；本机网络能否访问 api.archive-at-home.org</td></tr>
        <tr><td>认证失败</td><td>Node ID 与 Signature 是否来自同一组分配；Signature 的 Base64 是否完整复制</td></tr>
        <tr><td>任务持续失败</td><td>大概率是 Cookie 失效：重新登录 E-Hentai 复制最新 Cookie，确认含 <code>ipb_member_id</code> / <code>ipb_pass_hash</code></td></tr>
        <tr><td>GP 消耗异常</td><td>检查 <code>max_gp_cost</code> 与 <code>gp_cost_willingness</code> 设置；Dashboard 可看逐任务消耗</td></tr>
      </tbody>
    </table>

    <div class="callout callout-info">
      <span class="callout-icon">i</span>
      <p>
        多节点、多地区部署可提高网络的并发能力与稳定性。
      </p>
    </div>
  </div>
</BaseLayout>
